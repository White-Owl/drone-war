' The rabbit drone.
' this time it is written in the Drone-Basic dialect

MyHealth = health()
start:
	do
		call sleep(10)
	loop while MyHealth = health()
	call move(rnd(0, 360))
	call sleep(10)
goto start
